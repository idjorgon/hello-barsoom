name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  name:
    description: 'Name'
    required: true
    default: 'Barsoom'
outputs:
  greeting:
    description: 'We greeted you'
runs:
  using: 'node20'
  main: 'index.js'
